// -*- mode: jsonc -*-
[
  {
    "height": 32,
    "spacing": 4,
    "output": ["DP-2", "eDP-1"],
    "include": ["~/.config/waybar/config-modules.jsonc"],
    "modules-left": [
      "hyprland/workspaces",
      // "wlr/taskbar",
      "hyprland/window",
    ],
    "modules-center": ["clock", "idle_inhibitor", "custom/cava"],
    "modules-right": [
      // ↓ each of these is one line — comment it out to disable that module
      "custom/printer", // 3D printer progress (also set ENABLED=0 in ~/.scripts/waybar-printers.sh)
      "network", // Wi-Fi dropdown
      "bluetooth", // Bluetooth dropdown
      "battery", // hides itself on the desktop (no battery)
      "pulseaudio",
      "cpu",
      "memory",
      "temperature",
      "custom/power",
    ],
  },
  {
    "height": 32,
    "spacing": 4,
    "output": ["HDMI-A-2"],
    "include": ["~/.config/waybar/config-modules.jsonc"],
    "modules-left": ["hyprland/window"],
    "modules-center": ["clock"],
    "modules-right": ["tray"],
  },
]
